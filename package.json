{
  "name": "Foundationappsubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ pdrok/Foundationappsubgraph",
    "create-local": "graph create --node http://localhost:8020/ pdrok/Foundationappsubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ pdrok/Foundationappsubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pdrok/Foundationappsubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
